#!/usr/bin/env node

const path = require('path');
const { homedir } = require('os');

const nodeHelp = require(path.join(__dirname, '..', 'src'));
const updateScript = require(path.join(__dirname,'..','scripts','update'));
const nodeHelpConfigPath = path.join(homedir(), '.node-help');

nodeHelp();

/*

FIX THIS CONFUSING MESS

const updateFirst = process.argv.includes('--update') || process.argv.includes('-u'); 
const noRun = process.argv.includes('--no-run') || process.argv.includes('-n');

if (updateFirst) {
    if (noRun)
        updateScript(process.exit);
    updateScript(nodeHelp);
}
else {
    nodeHelp();
}
*/
